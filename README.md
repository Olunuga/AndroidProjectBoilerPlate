**Guildes to use this repository**

## clone Repository
git clone url/to/git

## set remote url to your own git repo
git remote set-url "url/to/git"

## Rebase the master branch
git pull —rebase origin mater

## Start making changes :-).
